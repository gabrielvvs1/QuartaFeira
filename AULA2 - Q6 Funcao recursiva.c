#include <stdio.h>
#include <stdlib.h>

#define ERRO -1 // EXERCICIO PENDENTE

int f (int X);

int main(){
    system("cls");
    int n = 3;
    int resposta = g(n);
    if (resposta != ERRO) printf("g")

    
}